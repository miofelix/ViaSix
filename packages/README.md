# Shared packages

跨端可复用库的占位目录。

**现阶段**：配置投影仍以各端实现 + `contracts/fixtures` 对齐为主，不强制共享语言运行时。

**后续可选**：

- `mihomo-config` — 若投影规则维护成本升高，可抽成 Rust/Go 单实现并通过 FFI 供各端使用。
- `domain` — 就绪条件、错误码、路由模式枚举等纯逻辑。

在未落地共享库前，请勿在此目录添加会被某端强制依赖的半成品代码。
